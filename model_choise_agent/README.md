# Model Choice Agent

Интеллектуальный агент для автоматического выбора и настройки LLM-моделей на основе анализа сложности задачи. Проект интегрирован с **SGR Deep Research** и предоставляет инструменты для динамического выбора оптимальной модели машинного обучения.

## 📋 Описание

**Model Choice Agent** — это система автоматизации выбора LLM-моделей, которая:

- **Анализирует сложность задачи** пользователя и определяет требуемый тип модели
- **Выбирает оптимальную конфигурацию** (light/heavy/agent) на основе анализа
- **Настраивает параметры модели** (temperature, max_tokens) в зависимости от типа результата (strict/creative)
- **Выполняет вызов выбранной модели** и возвращает финальный ответ

Проект построен на базе **SGR (Step-Guided Reasoning)** — фреймворка для глубоких исследований с пошаговым рассуждением.

## 🏗️ Архитектура

### Компоненты проекта

```
model_choise_agent/
├── tools/                    # Инструменты агента
│   ├── prompt_access_tool.py    # TaskAccessTool - анализ задачи
│   ├── llm_config_tool.py       # ConfigModelTool - получение конфигурации
│   └── llm_call_tool.py         # LLMCallTool - вызов модели
├── services/                 # Сервисы
│   └── llm_service.py          # LLMService - управление LLM клиентами
├── prompts/                  # Промпты для инструментов
├── config.yaml              # Конфигурация моделей
└── register_tools.py         # Регистрация инструментов в SGR
```

### Инструменты агента

#### 1. **TaskAccessTool** — Анализ задачи

**Обязательный первый шаг** — анализирует промпт пользователя и определяет:

- **model_kind**: Тип требуемой модели
  - `light` — модели до 10B параметров без инструментов (простые задачи)
  - `heavy` — модели свыше 10B параметров без инструментов (сложные задачи)
  - `agent` — модели с подключенными инструментами (задачи, требующие внешних данных)

- **result_kind**: Тип результата
  - `strict` — низкая temperature для фактологических, точных ответов
  - `creative` — высокая temperature для творческих, вариативных ответов

Использует инструментальную модель (настраивается в `config.yaml`) для анализа промпта.

#### 2. **ConfigModelTool** — Получение конфигурации

Извлекает конфигурацию модели из `config.yaml` на основе результатов `TaskAccessTool`:

- `base_url`, `model`, `api_key`
- `max_tokens`, `temperature` (в зависимости от `result_kind`)
- `system_prompt` (загружается из файла)

#### 3. **LLMCallTool** — Вызов модели

**Финальный шаг** — выполняет фактический вызов выбранной LLM-модели:

- Принимает конфигурацию от `ConfigModelTool` и оригинальный промпт пользователя
- Вызывает модель через `LLMService`
- Поддерживает streaming для фронтенда
- Извлекает `FinalAnswerTool.answer` из ответа агента

## 🔄 Workflow

Типичный workflow агента:

```
1. Пользователь отправляет запрос
   ↓
2. TaskAccessTool анализирует запрос
   → Определяет model_kind и result_kind
   ↓
3. ConfigModelTool получает конфигурацию
   → Извлекает параметры из config.yaml
   ↓
4. LLMCallTool вызывает выбранную модель
   → Передает промпт и конфигурацию
   → Получает ответ от модели
   ↓
5. FinalAnswerTool возвращает финальный ответ
   → Извлекается из ответа агента
   → Отображается во фронтенде
```



## 🚀 Использование

### Предварительные требования

- Python 3.11+ (для серверной части)
- Node.js 20.19.0+ или 22.12.0+ (для фронтенда)
- Настроенный `config.yaml` с API ключами для моделей

### Запуск системы

#### 1. Запуск сервера SGR Core

Серверная часть запускается из корневой директории проекта `sgr_deep_research`:

```bash
# Установка зависимостей (если еще не установлены)
pip install -e .

# Запуск сервера
python -m sgr_deep_research
```

Или если установлен как пакет:

```bash
sgr
```

Сервер будет доступен по адресу `http://localhost:8010` (порт по умолчанию).

**Параметры запуска:**
- `--host` — хост для прослушивания (по умолчанию: `0.0.0.0`)
- `--port` — порт (по умолчанию: `8010`)
- `--config-file` — путь к файлу конфигурации (по умолчанию: `config.yaml`)
- `--agents-file` — путь к файлу определений агентов (по умолчанию: `agents.yaml`)

**Пример:**
```bash
python -m sgr_deep_research --port 8010 --config-file config.yaml --agents-file agents.yaml
```

#### 2. Запуск фронтенда

Фронтенд запускается из директории `sgr-deep-research-frontend`:

```bash
# Переход в директорию фронтенда
cd sgr-deep-research-frontend

# Установка зависимостей (если еще не установлены)
npm install

# Запуск dev-сервера
npm run dev
```

Фронтенд будет доступен по адресу `http://localhost:5173` (порт по умолчанию для Vite).

**Важно:** Убедитесь, что сервер SGR Core запущен перед запуском фронтенда, так как фронтенд отправляет запросы к API сервера.

#### 3. Использование Model Choice Agent

После запуска сервера и фронтенда:

1. **Откройте браузер** и перейдите по адресу `http://localhost:5173`

2. **Выберите агента** в выпадающем списке:
   - Найдите и выберите **"Model Choice Agent"** (или `model_choice_agent`)
   - Агент должен быть зарегистрирован в `agents.yaml` с инструментами:
     ```yaml
     tools:
       - "TaskAccessTool"
       - "ConfigModelTool"
       - "LLMCallTool"
       - "FinalAnswerTool"
     ```

3. **Введите запрос** в поле ввода:
   - Например: *"Напиши эссе о влиянии искусственного интеллекта на образование"*
   - Или: *"Какая столица Франции?"*

4. **Отправьте запрос** и наблюдайте за процессом:
   - Агент автоматически проанализирует запрос через `TaskAccessTool`
   - Выберет подходящую модель через `ConfigModelTool`
   - Вызовет модель через `LLMCallTool`
   - Вернет финальный ответ через `FinalAnswerTool`

5. **Результат** отобразится в интерфейсе:
   - Вы увидите все этапы выполнения в реальном времени (streaming)
   - Финальный ответ будет отображен в специальном блоке с markdown-форматированием

### Примеры запросов

**Простая задача (light модель):**
```
Какая столица Японии?
```

**Сложная задача (heavy модель):**
```
Напиши развернутое эссе о влиянии квантовых вычислений на криптографию
```

**Задача с внешними данными (agent модель):**
```
Найди последние новости о разработках в области искусственного интеллекта
```

### Проверка работоспособности

После запуска сервера проверьте доступность API:

```bash
# Проверка health check
curl http://localhost:8010/health

# Получение списка доступных агентов
curl http://localhost:8010/v1/models
```

В ответе должен быть список агентов, включая `model_choice_agent`.

## ⚙️ Конфигурация

### config.yaml

Основной файл конфигурации содержит настройки для всех типов моделей:

```yaml
# Инструментальная модель (для анализа промптов)
llm:
  api_key: "..."
  base_url: "https://llm.api.cloud.yandex.net/v1"
  model: "gpt://.../yandexgpt-lite/latest"
  temperature: 0.3
  max_tokens: 16000

# Легкие модели
llm_light:
  api_key: "..."
  base_url: "..."
  model: "..."
  max_tokens: 4000
  temperature_strict: 0.1
  temperature_creative: 0.8
  system_prompt_path: "prompts/system_prompt_light.txt"

# Тяжелые модели
llm_heavy:
  # ... аналогично

# Агент-модели (с инструментами)
llm_agent:
  model: "sgr_openrouter_agent"
  base_url: "http://localhost:8010/v1"
  # ... аналогично
```




Проект является частью экосистемы SGR Deep Research.

## 🔗 Связанные проекты

- [SGR Deep Research](https://github.com/vamplabAI/sgr-agent-core) — базовый фреймворк
- [SGR Deep Research Frontend](https://github.com/...) — фронтенд приложение



